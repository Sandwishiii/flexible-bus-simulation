# 更新日志

本文件记录灵活公交仿真平台的所有重要变更。

---

## v0.4.0 (2026-08-17)

### 新增
- **导航轨迹回放**：车辆移动（接驾/乘车/巡游/回车）全部支持沿高德导航 polyline 行驶，不再是直线插值
- **本地文件缓存 (NavCache)**：`utils/nav_cache.py` — 用本地 JSON 文件缓存导航 API 结果，代替 Redis
  - 坐标 4 位精度（≈11m），7 天 TTL 自动过期
  - 延迟批量写入（每 5 秒最多刷写一次），`atexit` 自动落盘
  - 线程安全，全局单例模式
- **API 智能限流**：高德导航 API 调用改为随机 0.3~2s 间隔 + 连续错误指数退避
- **距离矩阵进度回调**：构建距离矩阵时前端实时显示进度（"构建距离矩阵... 50/190"）

### 修复
- **缓存命中也被限流**：`driving()` 方法中 `_throttle()` 移到缓存查询之后，缓存命中时 0 延迟直接返回
- **乘车移动无轨迹**：`_on_order_pickup` 中乘车段未传入 polyline，导致车辆直线飞行
- **巡游/回车无轨迹**：`_apply_stop_strategy` 中巡游和回车段未传入 polyline
- **乘车时间未使用导航**：`_get_ride_time` 调用未传入 `order` 参数，无法使用导航时间

### 改动
- `navigation.py`：`extensions` 从 `"base"` 改为 `"all"`，获取完整轨迹数据
- `distance_matrix.py`：新增 `get_distance_by_coord` / `get_duration_by_coord` / `get_polyline_by_coord` 方法
- `engine.py`：新增 `_estimate_cruise_time` 辅助方法，统一巡游/回车时间估算
- 项目结构新增 `cache/` 目录（自动生成，已加入 `.gitignore`）

### 技术细节
- 200 订单 + 5 辆车 + 20 站点场景下，首次运行约 300-500 次 API 调用（~8-15 分钟），再次运行全部命中缓存（0 次调用）
- 高德免费配额约 5000 次/Key/天，可支撑每日 10+ 次仿真

---

## v0.3.0 (2026-08-16)

### 新增
- **Web 平台完整交互**：FastAPI 后端 + 高德地图前端，完整参数配置和仿真流程
- **轨迹回放增强**：公交车图标 + 车号标签 + 状态圆点 + 车辆筛选 + 点击弹窗
- **双层轨迹线**：虚线（全程路径）+ 实线（已走路径）高亮显示
- **CSV 导出**：仿真指标报告导出含仿真参数
- **产品介绍帮助**：右上角 ? 按钮，Markdown 渲染弹窗
- **样例数据一键加载**：OD / 站点 / 区域边界

### 修复
- 车辆接驾后位置不更新（改为按速度逐步移动 + 轨迹插值）
- 评分矩阵过滤 mask 未完全生效
- CSS 选择器过宽导致样式意外覆盖

---

## v0.2.0 (2026-08-15)

### 新增
- **Web 平台基础**：FastAPI + 高德地图 2.0 前端
- **数据上传**：OD CSV / 站点 CSV+SQL / 区域 WKT
- **OD 可视化**：地图上显示 OD 线条（热力着色）
- **仿真运行**：后台线程 + 进度轮询
- **导航距离**：高德路径规划 API 封装，多 Key 池自动切换
- **距离矩阵**：站点间距离/时间预计算
- **车辆移动跟踪**：按速度逐步移动，轨迹插值

---

## v0.1.0 (2026-08-14)

### 新增
- **仿真引擎**：离散事件仿真 (PEDS)，事件驱动，优先队列调度
- **派单引擎**：完整派单流程（筛选→过滤→算分→匹配→执行）
- **二分图匹配**：KM 算法 (scipy) + 贪心匹配
- **三层优先级评分**：P1 车内乘客保障 + P2 缩短候车 + P3 控制空载
- **车辆/订单/站点模型**：dataclass + Enum 状态机
- **Demo 入口**：`main.py` — 杭州城西 12 站点 + 5 辆车演示
- **配置系统**：SimConfig + CityConfig (dataclass)
